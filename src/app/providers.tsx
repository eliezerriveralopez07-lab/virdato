"use client";

export default function Providers({ children }: { children: React.ReactNode }) {
  // Add your real providers here later (Theme, QueryClient, PostHog, etc.)
  return <>{children}</>;
}
