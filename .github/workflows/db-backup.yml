name: Nightly DB backup

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: db-backup
  cancel-in-progress: true

jobs:
  dump:
    runs-on: macos-latest          # TEMP: avoid Ubuntu queue
    timeout-minutes: 20
    steps:
      - name: Install pg client (macOS)
        run: |
          brew update
          brew install libpq
          brew link --force libpq
          pg_dump --version

      - name: Dump Postgres (compressed)
        env:
          DATABASE_URL_BACKUP: ${{ secrets.DATABASE_URL_BACKUP }}  # backup_ro URL
        run: |
          set -euo pipefail
          TS=$(date -u +%Y%m%d-%H%M%S)
          FILE="backup-${TS}.dump"
          pg_dump "$DATABASE_URL_BACKUP" -Z 9 -Fc -f "$FILE"
          echo "FILE=$FILE" >> "$GITHUB_ENV"

      - name: Debug listing
        run: |
          set -e
          ls -la
          echo "FILE is: $FILE"
          du -h "$FILE" || true

      - name: Upload artifact (90 days)
        uses: actions/upload-artifact@v4
        with:
          name: neon-backup
          path: ${{ env.FILE }}
          retention-days: 90

     
